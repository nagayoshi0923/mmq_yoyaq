-- =============================================================================
-- 20260611221000: create_private_booking_request の旧オーバーロードを削除
-- =============================================================================
-- p_private_group_id 追加前の9引数版が残存していた。
-- 9引数で呼ぶと組織解決バグ（20260611220000 で修正）を含む旧実装が実行されるため削除。
-- 現行フロントは常に p_private_group_id を渡すため10引数版のみが必要。
-- =============================================================================

DROP FUNCTION IF EXISTS public.create_private_booking_request(
  uuid, uuid, text, text, text, integer, jsonb, text, text
);
