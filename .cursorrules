# 開発ルール（互換ポインタ）

**正規ソースは `.cursor/rules/` です。** このファイルに詳細を書かない。

| 常時 | 領域別（必要時） |
|------|------------------|
| `00-core.mdc` | `multi-tenant.mdc` / `database.mdc` / `frontend.mdc` / `design.mdc` |
| `git-deploy.mdc` | `delivery-lanes.mdc` / `commands.mdc` |

地図: `docs/agent/INDEX.md`  
ツール別エントリ: `CLAUDE.md`（Claude/Cursor）・`AGENTS.md`（Codex）
