# 公演担当のID保持（QW-20260917-001）

スタッフ改名後に `gms` だけが変わり、`gm_roles` が旧名に残るため、受付等が通常GMとして扱われる経路があった。公演担当のスタッフID・役割・表示順を `schedule_event_staff_assignments` に保持し、給与はこの関係を読む。

旧クライアント、貸切確定RPC、コピー、インポートの名前入力は、公演の書込みトリガーで同じトランザクション中に変換する。一般的な逆方向トリガーは置かない。スタッフ改名だけは専用トリガーで既存の名前配列と役割キーを同時更新する。既存の expected_updated_at 条件はそのまま維持する。

- 一意に照合できた担当だけstaff_idを設定。未登録名と重複名は元の順序・名前・役割を残す。
- 新規の未登録名、重複、別組織スタッフ、不正な役割、改名後の古い役割キーを拒否。既存の未照合履歴は編集可能だが、後から同名スタッフが増えても勝手に結び付けない。
- 担当履歴のあるスタッフ削除を制約で防止し、非アクティブ化を案内。公演削除は従来どおり関連担当行も削除する。
- 新関係はブラウザから直接読めず、認証済み組織で絞ったサーバーAPIのみが取得する。
- 未確認担当者を給与画面に表示。未解決公演または担当者がある月の合計を参考値と明記する。報酬制度や既存の日付別履歴選択は変更しない。

検証環境: 14,627担当枠を保持（解決12,070、未登録2,541、重複16枠）。実データの改名をロールバック付きで検証し、全ID・役割の不変と旧表示の追従を確認。unit 448件、verify（型・lint・build・安全チェック）、DB回帰（改名、古いキー、他組織、失敗時取り消し、0名、削除、復旧）を通過。

本番残件: 作品不明の2026年9月1日・11日・15日・24日・25日は、作品と給与対象の事実確認待ち。過去の未登録担当も別人へ推測で割り当てない。予約のassigned_staff/gm_staffは既存の名前互換を継続し、旧配列の廃止や全入力のID専用化は未実施。正規化を追加しただけで構造改善全体完了とはしない。

移行: `supabase/migrations/20260921120000_event_staff_identity.sql`。
復旧: 旧版アプリへ戻してから `supabase/rollbacks/20260921120000_event_staff_identity.sql`。既存の公演・名前・役割を残したまま新関係のみ取り消す。


レビュー反映: 20260921123000で、孤児の役割キーがあり現担当の役割が明示されていない枠をrole_confirmed=falseにする。役割を旧名から推測せず、確認まで給与集計・CSVから除外する。重複を1人へ修正した場合は組織内の現スタッフへ再解決できる。月次インポートはDB書込み前に未登録名・重複名を公演別に表示して修正を求め、意図しない部分保存を避ける。未登録履歴を現スタッフへ結び直す操作は、担当を一旦外して保存後、確認済みのスタッフを再選択して保存する。未登録名と同名の新規スタッフへ自動割当しないための手順であり、元の記録は公演監査履歴に残る。

配備順: 20260921120000と20260921123000を先にDBへ適用してからAPI/画面を配備する。20260921120000はstaging適用済みで変更しない。修正が必要な場合は後続migrationを追加する。第2段階の本番配備はまだ行っていない。

2026-09-27 再検証：最新main（スタッフ退職/権限・予約人数・貸切認証等）と統合。未対応レビューを補正し、スタッフ一覧の読込待ちと失敗時の再試行、検証エラー時のプレビュー維持、保存対象外の重複行を除いてからの担当検証を追加。React実コンポーネント4件を含む679 unit・verify成功。役割確認列追加後にもPostgREST schema reloadを通知する。本番事前確認は15,645枠、スタッフ同名重複0、役割は許可済み5種のみ。実移行と本番受入はまだ未実施。

追加レビュー補強（09000）：公演編集がスタッフ改名と競合した場合、スタッフFKのロック待ちへ入る前にNOWAITで再試行を案内する。既にrole_confirmed=falseの担当は、孤児キー削除だけでは確認済みに戻さず、本人の役割が明示されるまで未確認を維持する。PGlite回帰と実staging別接続の改名/公演編集競合を検証し、元のID/役割保持・改名正常完了・fixture削除を確認。新規本番配備は120000→123000→20260927009000の順。
