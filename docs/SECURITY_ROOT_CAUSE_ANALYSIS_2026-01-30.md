# セキュリティ問題の根本原因分析と再発防止策

**作成日**: 2026-01-30  
**課題**: 同じようなセキュリティ問題が繰り返し発生している

---

## 問題の指摘

> 「P0の項目、同じように何度も修正してる気がするけどどうして毎回発生する？」

この指摘は正しい。以下のようなパターンが繰り返し発生している：

### 過去の類似問題（推測）

1. **RLS権限の緩さ** → 修正 → また別の箇所で同じ問題
2. **直接UPDATE** → 修正してRPC化 → 新機能でまた直接UPDATE
3. **クライアント入力を信頼** → 修正 → 別のEdge Functionで同じ問題
4. **非アトミック操作** → 修正 → 新しい機能でまた非アトミック

---

## 根本原因の分析

### 🔴 原因1: アーキテクチャガイドラインがない

**問題**: 
- 「予約データはRPC経由でのみ変更する」というルールが明文化されていない
- 新機能追加時に「どう実装すべきか」の判断基準がない
- 過去の修正の意図が伝わっていない

**証拠**:
```typescript
// ❌ こういうコードが繰り返し書かれる
await supabase.from('reservations').update({ 
  participant_count: newCount 
})

// なぜダメか、どうすべきか、が周知されていない
```

**影響**:
- 開発者Aが修正 → 開発者Bが同じミスを別の場所でする
- レビューでも気づかれない（基準がないため）

---

### 🔴 原因2: 型システムで防げていない

**問題**:
- TypeScriptの型定義がセキュリティを保証していない
- 危険な操作が型レベルでブロックされていない

**例**:
```typescript
// ❌ これがコンパイル通ってしまう
supabase.from('reservations').update({ status: 'cancelled' })

// ✅ こうあるべき
reservationApi.cancel(reservationId)  // RPC経由を強制
```

**現状**:
- `supabase.from('reservations')` が直接使える
- 危険な操作をラップしたAPI層が不完全

---

### 🔴 原因3: テストでカバーされていない

**問題**:
- セキュリティテストが自動化されていない
- 「顧客がstatusを変更できないか」をCIで検証していない

**証拠**:
```bash
# 存在しないテスト（あるべき姿）
tests/security/rls_policies.test.ts
tests/security/direct_update_prevention.test.ts
tests/security/authorization.test.ts
```

**影響**:
- リグレッション（過去の修正が壊れる）に気づかない
- 新機能のセキュリティホールが本番で発覚

---

### 🔴 原因4: RLS設計が防御的でない

**問題**:
- デフォルトが「許可」になっている
- 新しいテーブルを追加すると、RLSなしで使えてしまう

**現状**:
```sql
-- ❌ こうなっている
CREATE TABLE new_feature_table (...);
-- RLS設定を忘れると、全員がアクセス可能

-- ✅ こうあるべき
CREATE TABLE new_feature_table (...);
ALTER TABLE new_feature_table ENABLE ROW LEVEL SECURITY;
-- デフォルト拒否、明示的に許可を追加
```

---

### 🔴 原因5: コードレビューの観点が不明確

**問題**:
- 「セキュリティ観点でのレビュー」のチェックリストがない
- 何を確認すべきかが属人的

**欠けているレビュー項目**:
```
□ RLSポリシーは列単位で制限しているか？
□ 直接UPDATEではなくRPC経由か？
□ クライアント入力をサーバー側で検証しているか？
□ 複数DB操作がアトミックか？
□ organization_id フィルタを忘れていないか？
```

---

## 構造的な問題の整理

### 問題の階層

```
レイヤー1: アーキテクチャ設計（ルール不在）
    ↓
レイヤー2: 型システム（防御機構なし）
    ↓
レイヤー3: 実装（個別の脆弱性）← 今回修正したのはここだけ
    ↓
レイヤー4: テスト（検出できない）
    ↓
レイヤー5: レビュー（基準なし）
```

**今回の修正**: レイヤー3（実装）の穴を塞いだだけ  
**根本解決に必要**: レイヤー1〜5すべての改善

---

## 再発防止策（優先度順）

### 🟢 Phase 1: 今すぐできること（緊急）

#### 1-1. セキュアコーディングガイドライン作成

**ファイル**: `docs/SECURE_CODING_GUIDELINES.md`

**内容**:
```markdown
## 予約データの操作

### ❌ 禁止
- supabase.from('reservations').update() の直接使用
- supabase.from('schedule_events').update() の直接使用

### ✅ 必須
- reservationApi.updateParticipantsWithLock() を使用
- 新機能はRPC関数を作成してから実装

### 理由
- 在庫整合性の保証
- 権限チェックの一元化
- 監査ログの自動記録
```

#### 1-2. プルリクエストテンプレート更新

**ファイル**: `.github/PULL_REQUEST_TEMPLATE.md`

```markdown
## セキュリティチェック（必須）

- [ ] 新規テーブルにRLSポリシーを設定した
- [ ] 直接UPDATE/DELETEを使用していない（RPC経由）
- [ ] クライアント入力をサーバー側で検証している
- [ ] organization_id フィルタを設定している
- [ ] 複数DB操作をトランザクション化している
```

#### 1-3. ESLint/TSLintルール追加

**ファイル**: `.eslintrc.cjs`

```javascript
rules: {
  // 直接UPDATEを禁止
  'no-restricted-syntax': [
    'error',
    {
      selector: "CallExpression[callee.property.name='update'][callee.object.property.name='from']",
      message: 'Direct .from().update() is forbidden. Use API layer or RPC.'
    }
  ]
}
```

---

### 🟡 Phase 2: 2週間以内（重要）

#### 2-1. API抽象化層の完成

**目標**: 危険な操作を型レベルでブロック

```typescript
// src/lib/secureSupabase.ts
export const secureSupabase = {
  // 読み取りのみ許可
  from: (table: string) => ({
    select: supabase.from(table).select,
    // update, delete, insert は提供しない
  }),
  
  // 安全なAPI経由のみ
  reservations: reservationApi,
  scheduleEvents: scheduleEventApi,
}

// ✅ こうすることで
import { secureSupabase } from '@/lib/secureSupabase'
secureSupabase.from('reservations').update(...)  // コンパイルエラー
secureSupabase.reservations.updateParticipants(...)  // OK
```

#### 2-2. セキュリティテスト自動化

**ファイル**: `tests/security/rls.test.ts`

```typescript
describe('RLS Security', () => {
  it('顧客はstatusを変更できない', async () => {
    const { error } = await customerClient
      .from('reservations')
      .update({ status: 'cancelled' })
      .eq('id', myReservationId)
    
    expect(error?.code).toBe('PGRST301')  // RLS violation
  })
})
```

**CI統合**:
```yaml
# .github/workflows/security-tests.yml
name: Security Tests
on: [pull_request]
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - run: npm run test:security
```

---

### 🔵 Phase 3: 1ヶ月以内（基盤強化）

#### 3-1. マイグレーションテンプレート

**新規テーブル作成時**:
```sql
-- template_new_table.sql
CREATE TABLE new_table (...);

-- 🚨 必須: RLS有効化
ALTER TABLE new_table ENABLE ROW LEVEL SECURITY;

-- 🚨 必須: デフォルト拒否
-- （明示的にポリシーを追加するまでアクセス不可）

-- 🚨 必須: organization_id でフィルタ
CREATE POLICY new_table_org_isolation ON new_table
  USING (organization_id = get_user_organization_id());
```

#### 3-2. データベース制約の強化

```sql
-- 在庫整合性をDB制約で保証
ALTER TABLE schedule_events
ADD CONSTRAINT check_participants_valid
CHECK (current_participants >= 0 AND current_participants <= max_participants);

-- organization_id を NOT NULL に
ALTER TABLE reservations
ALTER COLUMN organization_id SET NOT NULL;
```

#### 3-3. 監査ログの完全化

```sql
-- すべての重要操作をログ記録
CREATE TABLE audit_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  table_name TEXT NOT NULL,
  operation TEXT NOT NULL,  -- INSERT, UPDATE, DELETE
  row_id UUID NOT NULL,
  old_data JSONB,
  new_data JSONB,
  user_id UUID,
  organization_id UUID,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- トリガーで自動記録
CREATE TRIGGER audit_reservations
AFTER UPDATE ON reservations
FOR EACH ROW EXECUTE FUNCTION audit_log_changes();
```

---

## 具体的なアクションプラン

### 明日から（担当: 開発チーム）

1. ✅ **このドキュメントを読む**（全員）
2. ✅ **セキュアコーディングガイドライン作成**（1時間）
3. ✅ **PRテンプレート更新**（30分）
4. ✅ **既存コードでの直接UPDATE検索**（30分）
   ```bash
   rg "\.from\('(reservations|schedule_events)'\)\.update\(" src/
   ```

### 今週中（担当: リードエンジニア）

5. ⬜ **ESLintルール追加と全コードスキャン**（2時間）
6. ⬜ **API抽象化層の設計レビュー**（1時間）
7. ⬜ **セキュリティテストの優先順位決定**（1時間）

### 2週間後（担当: チーム全体）

8. ⬜ **API抽象化層の実装完了**（3日）
9. ⬜ **セキュリティテスト20件追加**（2日）
10. ⬜ **全PRでセキュリティレビュー実施**（継続）

---

## 成功の指標（KPI）

### 短期（1ヶ月）
- ✅ 直接UPDATE が0件（ESLintで強制）
- ✅ セキュリティテストカバレッジ 50%以上
- ✅ 全PRでセキュリティチェックリスト実施

### 中期（3ヶ月）
- ✅ 新規P0脆弱性 0件
- ✅ セキュリティテストカバレッジ 80%以上
- ✅ マイグレーション時のRLS設定漏れ 0件

### 長期（6ヶ月）
- ✅ セキュリティインシデント 0件
- ✅ 監査ログ100%記録
- ✅ 自動セキュリティスキャン導入

---

## まとめ: なぜ繰り返すのか？

### 答え

**「実装レベルの問題を個別に直しているだけで、構造的な問題に手をつけていないから」**

### 今必要なこと

1. **ルールを明文化する**（ガイドライン）
2. **ルールを強制する**（型システム、Linter）
3. **ルールを検証する**（自動テスト）
4. **ルールを周知する**（PRテンプレート、レビュー）

### 投資対効果

| 対策 | コスト | 効果 | ROI |
|------|--------|------|-----|
| ガイドライン作成 | 1時間 | 大（迷わなくなる） | 最高 |
| ESLintルール | 2時間 | 大（強制力） | 最高 |
| PRテンプレート | 30分 | 中（意識向上） | 高 |
| セキュリティテスト | 2日 | 大（リグレッション防止） | 高 |
| API抽象化層 | 3日 | 中〜大（型安全） | 中 |

---

**次のアクション**: Phase 1（緊急）の4項目を今日中に実施

**担当者**: @開発リード  
**期限**: 2026-01-31
