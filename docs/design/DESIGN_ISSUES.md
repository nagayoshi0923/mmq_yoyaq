# デザイン改善 台帳（DI 台帳）

作成: 2026-07-03（Claude・全管理ページのコード監査 scout×5 ＋ 既存メモ統合）

## 使い方（運用ルール）

- **オーナーは番号で指示するだけ**:「DI-17 やって」「DI-05 と DI-06 まとめて」「DI-13 は却下」
- 状態: ⬜未判断 / 👍やる（承認済み・順番待ち） / 🔧対応中 / ✅済み / ❌やらない
- **オーナー追記**: 実機を見て気になった点は、該当ページの下にそのまま1行追記してください（書式自由）。AI が次の整理で DI-番号を振って指示可能な形に変換します。
- 各項目は「コード監査で見つけた候補」であり、**やるかどうかはオーナー判断**。見た目の最終判断は実機優先。
- 大規模なもの（★）はページ改装バッチ（ADMIN_UI_REDESIGN_PLAN）として実施。小粒はそれ単体で1コミット。
- 🔒 公演カード・PerformanceModal は対象外（見た目変更禁止）。顧客向けページも対象外（ブランド維持・トークン化のみ）。

## 監査カバレッジ（管理ページ全部見た証跡・2026-07-03 scout×6）

| サイドバー | ページ | 状態 |
|---|---|---|
| （トップ） | ダッシュボード / スケジュール（枠のみ） / 店舗 / スタッフ / シナリオ | DI-07〜12 |
| シフト・GM | シフト提出 / GM確認 / 担当作品 | DI-13〜15 |
| 貸切・予約 | 貸切管理（=改装済みパイロット・基準側） / グループ一覧 / 予約管理 | DI-16〜17 |
| 顧客・クーポン | 顧客（アカウント管理含む） / クーポン | DI-18〜23 |
| メール | メール送信ログ | 特記なし |
| コンテンツ | ブログ管理（/blog） | **良好・特記なし** |
| 運営マニュアル/使い方 | Manual（管理側） / GuidePage（顧客向けガイド） | DI-35 / 対象外注記 |
| 売上・管理 | 売上9タブ / 公演報告4タブ | DI-24〜28 |
| 設定 | 全17タブ＋組織設定 | DI-32〜34（概ね健全） |
| MMQ運営 | ユーザー管理 / テナント管理 / マスタ管理 / 外部公演報告 / ライセンス報告管理 / シナリオマッチャー | DI-29〜31（前3つは良好） |

---

## A. 横断（全ページ共通の癖。改装バッチ時に該当ページへ一括適用）

- ⬜ **DI-01** 色の直書き（text-gray-* / text-red-500 / bg-*-50 等）が全域に散在 → `text-muted-foreground` 等の token へ。ページ改装時に各ページぶんをまとめて置換（単独バッチはやらない）
- ⬜ **DI-02** ステータスバッジの独自実装・className 上書きが5箇所以上（ReservationManagement.tsx:148 / ExternalReports:39 / LicenseReportManagement:54 / CampaignList:79 / ReportsReceived:46）→ patterns のバッジ一元化に寄せる
- ⬜ **DI-03** 検索・フィルタの手書き実装が6ページ以上（予約管理・グループ一覧・売上概要・外部売上・公演報告・受信）→ `patterns/filter` の SearchInput / FilterBar へ
- ⬜ **DI-04** 空状態・ローディングの独自実装が5ページ以上（ダッシュボード・予約管理・グループ一覧・顧客管理・シナリオマッチャー）→ EmptyState / ListSkeleton へ
- ⬜ **DI-05** Button variant の上から色を className で上書き（ReservationManagement:386,410,519,678 / LicenseReportManagement:236,248）→ variant 機能に統一
- ⬜ **DI-06** `text-[10px]` 等の任意フォントサイズ（StaffManagement:535 / ScheduleManager:1245 / ReservationManagement:501）→ ts-* / text-xs 基準へ
- ⬜ **DI-39** ライセンス報告のステータスバッジ（getStatusBadge）と承認/却下ボタン色が ReportsReceived と LicenseReportManagement に**完全同一の重複実装**（2026-07-06 DI-28 調査で判明）→ 共通部品に切り出して両ページ一括統一。片方だけ変えるとズレるため DI-28 では意図的に未着手
- ✅ **DI-38** エクスポート/出力系ボタンの横並び廃止 → 共通 **ActionMenu**（patterns/action・1ボタン＋プルダウン）新設・適用 `695dfe3c`（オーナー要望 2026-07-04）。適用=売上概要4連ボタン（未実装Excelは削除）＋スケジュール管理の入出力2ボタン。単独CSVボタン各所とインポート/エクスポートペア部品は対象外と判断。**今後の改装バッチでも複数アクションはこの部品に寄せる**

## B. ページ別

### ダッシュボード（DashboardHome）
- ⬜ **DI-07** セクション見出しが h2 直書き×3＋ローディング手作り（:336,406,468 / :343-357）→ 見出し様式の統一＋ListSkeleton
- ⬜ **DI-08** 統計カードが独自枠（bg-card rounded-2xl・:493-501）→ StatCard 系へ

### スケジュール管理（ページ枠のみ・🔒カード/モーダル除外）
- ⬜ **DI-09** 曜日色 text-red-300/blue-300・日付バー bg-slate-700・列幅 w-[32px] 等のハードコード（index.tsx:1245-1268）→ 見た目そのままの token 化（挙動・見た目不変）
- ✅ **DI-37** フィルタ行で「店舗」だけ白背景＋枠線で浮く（オーナー指摘 2026-07-04 スクショ）→ StoreMultiSelect に triggerClassName/triggerStyle を追加し、デスクトップ＋モバイル両フィルタで他4つの MultiSelect（薄青フラット）と統一。他13使用箇所は無影響（プロップ未指定=従来どおり）。**教訓: 「同一行内のコンポーネント混在」は grep 監査で拾えない＝改装バッチ毎の実機スクショ確認が必須**

### 店舗管理
- ⬜ **DI-10** モバイルカードのフッター bg-gray-50 直書き＋ownership バッジが @ts-ignore で variant 上書き（index.tsx:343-360）

### スタッフ管理
- ⬜ **DI-11** モバイルカード内バッジの色直書き（bg-amber-50/blue-50/green-50・:483,510,524）＋ text-[10px]

### シナリオ管理（一覧のみ）
- ⬜ **DI-12** 凡例の bg-gray-100 / bg-blue-100 / text-gray-500 直書き（index.tsx:162-170）

### シフト提出
- ✅ **DI-13** 提出期間案内バーの border-l 系 → 薄背景 tint 化（`2e2f57a1`・スモーク49 OK）

### GM確認
- ⬜ **DI-14** 空状態が Card 直書き（index.tsx:144-148）→ EmptyState

### 担当作品（StaffProfile）
- ⬜ **DI-15** CircleCheck の text-gray 直書き（:33-36）・CardContent の p-4/p-8 乱立（:347-418）・text-blue-500/green-500 直書き（:381,390）

### 貸切グループ一覧（PrivateGroupList）
- ⬜ **DI-16** 空状態 div 直書き（:342-347）／フィルタ手書き（:279-339）／進捗バー inline style（:466）／STATUS_CONFIG の色直書き（:34,447）

### 予約管理（ReservationManagement）★最多指摘＝**改装パイロット②の本命**
- ✅ **DI-17** ★ページ全体の改装1バッチ `06323481`（スモーク59 オーナーOK・2026-07-04）: バッジ共通化・フィルタ共通部品化・スケルトン/空状態統一・色トークン化

### 顧客管理・アカウント管理
- ✅ **DI-18〜22** 顧客管理の改装1バッチで対応済み `325128db`+`81f7be3d`＋FB対応 `b32a8fc6`+`4bf3acf7`（スモーク60・61 オーナーOK・2026-07-04）: 二重ヘッダー解消・行展開セクション再構成・「もっと見る」折りたたみ・クーポン統合・色トークン化・スケルトン共通化。体験済みは統合1リスト・日付バッジ

### クーポン管理
- ✅ **DI-23** `a67afe15`（ListSkeleton/EmptyState＋統計 StatCard 化＋フォーム色トークン化。スモーク85-88 オーナーOK・2026-07-06）。カテゴリバッジ（:79 青tint）は意味のある薄背景tintとして維持。**Switch＋DropdownMenu は現状維持**: ActionMenu 部品がアイコン単独トリガー非対応のため（label+ChevronDown 固定）。混在が気になる場合は DI-40 で部品拡張から
- ⬜ **DI-40** ActionMenu 部品にアイコン単独トリガー（size=icon・label/Chevron なし）対応を追加 → 各一覧ページの MoreVertical DropdownMenu を集約可能にする（DI-23 実装中に判明した部品の制約）

### 売上管理
- ✅ **DI-24** ★売上概要 SummaryCards → StatCard 様式に統一 `5be2de35`（スモーク62 オーナーOK・2026-07-04）
- ❌ **DI-25** 外部売上 → タブ自体をメニューから撤去（オーナー指示 2026-07-04・M19 で再提案）につき対象外
- ❌ **DI-26** 雑収支管理 → 同上（タブ撤去・M19）につき対象外

### 公演報告（LicenseManagement）
- ✅ **DI-27** 公演報告タブ `6ea115bd`（SearchInput 化＋統計6枚 StatCard 化＋ListSkeleton/EmptyState。スモーク77-80 オーナーOK・2026-07-06）。※ReportGroupCard の緑/橙は送信済/未登録の機能色＋差分⚠️バッジ仕様保護のため意図的に対象外
- ✅ **DI-28** 受信タブ → `6ee83388`（統計4枚 StatCard 化＋FilterBar/SearchInput/FilterSelect＋ListSkeleton/EmptyState。スモーク81-84 オーナーOK・2026-07-06）。PageHeader は index 側にあるためタブには追加せず（DI-18 の方針）。バッジ・承認/却下ボタン色は DI-39 で一括統一予定

### MMQ運営
- ⬜ **DI-29** マスタ編集（ScenarioMasterEdit）: 生 h1/h2 見出し6箇所（:457-771）＋権限エラーが生テキスト → 見出し様式とエラーカードの統一
- ⬜ **DI-30** 外部公演報告: Card 外の h2 見出し（:123）＋バッジ色直書き（:39）
- ✅ **DI-31** ★シナリオマッチャー全面統一 `24ec7619`（PageHeader 化・外殻Card廃止・FilterBar+SearchInput・ListSkeleton/EmptyState・色トークン化。機能/数字不変。スモーク72-76 オーナーOK・2026-07-06）
- ※ ユーザー管理・テナント管理・マスタ管理一覧は監査で「良好」

### 設定タブ群（定型が効いていて概ね健全。個別3件のみ）
- ✅ **DI-32** ⚡通知設定の動的 Tailwind クラス → 静的マップ化（`2e2f57a1`・実バグだった=色が付いた・スモーク48 OK）
- ⬜ **DI-33** 売上レポート設定だけ Card 亜種（:198-259）→ 設定定型（bg-white rounded-xl border p-6＋SectionTitle）へ
- ✅ **DI-34** 組織デザイン設定の禁止 border-l-4 → 薄背景 tint 化（src 全体7箇所と共に全廃・`2e2f57a1`・スモーク49 OK）

### 運営マニュアル（Manual/index.tsx・管理側）
- ⬜ **DI-35** PageHeader 不使用（:498-507 自前タイトル）＋ DbPageViewer のローディング/空状態が生テキスト（:267）→ 見出し様式＋ListSkeleton/EmptyState へ。DemoFrame の点線枠（:289）も Card 化余地
- ※ ブログ管理（/blog）は良好・特記なし
- ※ GuidePage（顧客向け使い方ガイド）は**改装対象外**: PublicLayout 配下の顧客ページで、hex 直書き・角丸ゼロは意図的ブランド、border-l 系は実UI（スケジュール画面）を模したデモ画面のため。触るなら D-3（見た目不変トークン化）の範疇のみ

### 顧客向け・貸切グループチャット（GitHub #274 より移管）
- ⬜ **DI-36** グループチャットに「店舗は原則メッセージ内容を確認しません」の明示を追加（問い合わせがチャットに書かれて気づけない事故の防止。チャット入力欄付近 or 初回案内メッセージ）

---

## C. オーナー追記欄（自由記入。AI が DI 番号化します）

- （ここに実機で気になったことを1行ずつ）

---

## 進め方（2026-07-04 整理・オーナーはこれだけ見ればOK）

**基本ループ（1ページ = 1周）:**

1. Claude がページを1つ改装して staging に push（機能・数字は不変、見た目だけ）
2. オーナーはそのページを**1回見る** → 返事は「**OK**」か「**気になる箇所のスクショ**」だけ（DI 番号は覚えなくていい・台帳管理は Claude の仕事）
3. OK なら Claude が次のページへ。スクショが来たら直してから次へ

**ページの順番（ワースト順・Claude が上から消化）:**

1. ✅ 貸切管理（パイロット・済み。「違和感なし」＝このトーンが基準）
2. ✅ 予約管理（DI-17・スモーク59 OK）
3. ✅ 顧客管理（DI-18〜22・スモーク60/61 OK。指摘4点込みで対応済み）
4. ✅ 売上概要（DI-24・スモーク62 OK。外部売上・雑収支はタブ撤去で対象外）
5. 🔜 **シナリオマッチャー**（DI-31・2026-07-06 着手） → 残りの小粒ページ（公演報告 DI-27/28・クーポン DI-23・ダッシュボード DI-07/08 ほか）
6. 設定群・ユーザー管理系は健全なので最後

**変わらないもの（約束）**: 🔒公演カード・公演モーダル / 顧客向けページの見た目 / すべての数字・件数・並び順・動作
